{
  "name": "gpg-windows-relay-monorepo",
  "description": "GPG Windows Relay - Three extension pack for relaying GPG to Windows from remotes",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "compile": "npm run compile:shared && npm run compile:agent && npm run compile:request",
    "compile:shared": "cd shared && npm run compile",
    "compile:agent": "cd agent-proxy && npm run compile",
    "compile:request": "cd request-proxy && npm run compile",
    "postinstall": "npm run postinstall:shared && npm run postinstall:agent && npm run postinstall:request",
    "postinstall:shared": "cd shared && npm install",
    "postinstall:agent": "cd agent-proxy && npm install",
    "postinstall:request": "cd request-proxy && npm install",
    "watch": "npm run watch:shared & npm run watch:agent & npm run watch:request",
    "watch:shared": "cd shared && npm run watch",
    "watch:agent": "cd agent-proxy && npm run watch",
    "watch:request": "cd request-proxy && npm run watch",
    "package": "npm run package:agent && npm run package:request && npm run package:pack",
    "package:agent": "cd agent-proxy && npm run package",
    "package:request": "cd request-proxy && npm run package",
    "package:pack": "cd pack && npm run package",
    "test": "npm run test:shared && npm run test:agent && npm run test:request",
    "test:shared": "cd shared && npm test",
    "test:agent": "cd agent-proxy && npm test",
    "test:request": "cd request-proxy && npm test",
    "test:watch": "cd shared && npm run test:watch",
    "clean": "rimraf -g \"agent-proxy/out\" \"request-proxy/out\" \"agent-proxy/gpg-agent-proxy-*.vsix\" \"request-proxy/gpg-request-proxy-*.vsix\" \"pack/gpg-windows-relay-*.vsix\" \"shared/out\" \"shared/**/*.d.ts\" \"shared/**/*.d.ts.map\""
  },
  "devDependencies": {
    "rimraf": "^5.0.0",
    "mocha": "^10.5.0",
    "@types/mocha": "^10.0.10"
  },
  "dependencies": {
    "@types/node": "^25.2.1",
    "typescript": "^5.9.3"
  }
}
